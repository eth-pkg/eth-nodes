Index: eth-node-nimbus-eth2-24.7.0/beacon_chain/version.nim
===================================================================
--- eth-node-nimbus-eth2-24.7.0.orig/beacon_chain/version.nim
+++ eth-node-nimbus-eth2-24.7.0/beacon_chain/version.nim
@@ -11,16 +11,23 @@
 ## in the Nimbus release process (i.e. beacon_node, validator_client, etc)
 
 import std/[strutils, compilesettings]
+import os
 
 const
   compileYear = CompileDate[0 ..< 4]  # YYYY-MM-DD (UTC)
+  reproducibleYear = getEnv("BUILD_YEAR", "2024")
   copyrights* =
-    "Copyright (c) 2019-" & compileYear & " Status Research & Development GmbH"
+    "Copyright (c) 2019-" & reproducibleYear & " Status Research & Development GmbH"
 
   versionMajor* = 24
   versionMinor* = 7
   versionBuild* = 0
 
+  versionMajor* = parseInt(getEnv("VERSION_MAJOR", versionMajor))
+  versionMinor* = parseInt(getEnv("VERSION_MINOR", versionMinor))
+  versionBuild* = parseInt(getEnv("VERSION_BUILD", versionBuild))
+
+
   versionBlob* = "stateofus" # Single word - ends up in the default graffiti
 
   ## You can override this if you are building the
@@ -32,9 +39,11 @@ const
       # compile-time error.
       # We work-around the problem with this override and
       # save some time in executing the external command.
-      "123456"
+      gitRevisionOverride = getEnv("GIT_REVISION", "123456")
+      gitRevisionOverride
     else:
-      ""
+      gitRevisionOverride = getEnv("GIT_REVISION", "123456")
+      gitRevisionOverride
 
   gitRevisionLong* = when git_revision_override.len == 0:
     staticExec "git rev-parse --short HEAD"
