#!/bin/bash
set -e

. /usr/share/debconf/confmodule

declare -A CONFIG

adduser --system --quiet --group eth-node-lodestar-testnet 
mkdir -p /var/lib/eth-node-testnet/lodestar
chown eth-node-lodestar-testnet:eth-node-lodestar-testnet /var/lib/eth-node-testnet/lodestar 

jwt_file=/etc/eth-node-testnet/jwt.hex
if [ ! -f "$jwt_file" ]; then
	openssl rand -hex 32 | tr -d "\n" | tee "$jwt_file" >/dev/null
fi

if [ "$1" = triggered -a "$service_was_running" '!=' 0 ];
then
	deb-systemd-invoke restart eth-node-testnet-service-lodestar
fi

if [ "$1" '!=' triggered ];
then
	dpkg-trigger eth-node-testnet-service-lodestar-config-changed

fi
#DEBHELPER#

exit 0
