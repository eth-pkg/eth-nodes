name: Package eth-node-lodestar on debian 12

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main     
    paths:
      - 'debian-12/eth-node-lodestar/**'  

jobs:
  package:
    strategy:
      matrix:
        package-name: ["eth-node-lodestar"]
        package-version: ["1.11.3-1"]
        codename: ["debian-12"]
    uses: ./.github/workflows/pkgbuilder-deps.yml
    with:
      package-name: ${{ matrix.package-name }}
      package-version: ${{ matrix.package-version }}
      codename:  ${{ matrix.codename }}

  